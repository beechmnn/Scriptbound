# Coding agent instructions

- Run `npm run format` after every code or configuration change and before handing work back.
- Run `npm run lint`, `npm run check`, and the relevant tests before completing a coding task.
- Do not manually reformat generated output in `.svelte-kit/` or `build/`.
